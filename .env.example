# Variables d'environnement pour Docker
# Ce fichier sera automatiquement copié en .env par les scripts d'installation

# Configuration utilisateur Docker (Linux/macOS)
# Sur Windows, ces valeurs sont fixes à 1000
DOCKER_USER_ID=1000
DOCKER_GROUP_ID=1000
DOCKER_USERNAME=rails

# Configuration base de données (définie dans docker-compose.yml)
# Ces variables sont déjà configurées, pas besoin de les modifier
# POSTGRES_DB=rails_development
# POSTGRES_USER=rails
# POSTGRES_PASSWORD=password
