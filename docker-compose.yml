version: "3.9"

services:
  web:
    build: .
    command: bash -c "bundle install && bin/rails db:migrate && bin/rails server -b 0.0.0.0 -p 3000"
    volumes:
      - .:/app
      - bundle:/bundle
    ports:
      - "3000:3000"
    environment:
      - RAILS_ENV=development

volumes:
  bundle: {}
